import PublicChannelEntryPage from "@/components/channel-entry/PublicChannelEntryPage";

export const dynamic = "force-dynamic";

export default function ChannelEntryRoutePage() {
  return <PublicChannelEntryPage />;
}
